# Jev 将棋

`shogi.cmd` をダブルクリック。あなたが先手、Jevが後手です。
モデル指定は `jev-preview`。実際の応答モデルIDも棋譜JSONに記録します。
2026-09-22確認時の公式ドキュメントではpreviewとlatestは同じ `jev-1.13.0` です。
自分の駒→移動先をクリック。持ち駒もクリックして打てます。
成れる場合は「成る／成らない」ボタンで選択します。
「待った」はあなたの前の手まで戻します。通信中は閉じる以外の操作を待ちます。

JevにはSFEN、文字盤面、持ち駒、直近20手、合法手一覧を渡します。
候補が255手を超えるとグループごとに選び、最後に候補同士から選びます。
各候補に王手・詰み・取る駒・成りの価値・相手の最善の駒取りと同じマスへの取り返しを添えます。
駒の損得は盤上の価値と持ち駒を含む独自の簡易評価（歩1、香桂3、銀5、金6、角8、飛10、馬11、龍13）です。
取った駒は成る前の価値で持ち駒に加わります。王は駒得の数値で評価せず、合法手・詰みで扱います。
長い交換、駒を取らない脅威、数手先の詰みは読んでいません。損失0は安全の保証ではありません。
専用将棋エンジンの評価は使っていません。棋力の改善は未検証です。
`decisions.jsonl` に送信した局面と全候補の補助情報・選択結果・前処理時間を記録します。
APIエラー時は自動再試行せず「再試行」ボタンを表示します。
既存の暗号化したキーを利用し、キーをログに残しません。
`runs/shogi-日時/game.json` にUSI指し手と最終局面を保存します。

合法手判定は python-shogi 1.1.1（GPL-3.0）を利用。
https://github.com/gunyarakun/python-shogi
このPoCでは4回の同一局面を引き分けにし、連続王手の千日手の反則判定・入玉宣言・持将棋の点数判定は未実装です。
インストール: `.venv\Scripts\python.exe -m pip install -r requirements-shogi.txt`
