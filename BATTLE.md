# Jev Party — 4職の自動戦闘

```powershell
.\battle.cmd
```

起動すると戦闘を開始します。Spaceで一時停止／再開、Escまたは閉じるボタンで終了。
`--paused` を付けると開始前に止められます。

## チーム

| 職 | HP | 技 |
|---|---:|---|
| タンク | 240 | 単体攻撃10、挑発2秒、防御4秒、割り込み、味方をかばう |
| アーチャー | 90 | 単体攻撃20、ステルス、退避射撃 |
| メイジ | 85 | 味方強化、敵弱体、範囲攻撃22、足止め |
| ヒーラー | 100 | 単体回復30、障壁、緊急回復55 |

現在は **TACTICAL / tactical-v3** です。HARD版の敵HP・攻撃力を維持し、味方の技と敵の予備動作を追加しました。

## 新しい選択肢

| 技 | 効果・代償 | 個別の再使用待ち |
|---|---|---:|
| タンク：割り込み | 240px以内の敵へ接近し、予備動作中の攻撃を中断。1.5秒スタン | 7秒 |
| タンク：かばう | 285px以内の味方に接近。3秒間、160px以内ならダメージ70%を引き受ける | 9秒 |
| アーチャー：ステルス | 2.5秒間ターゲット不可。既に向かっている攻撃も回避するが、自分も攻撃できない | 10秒 |
| アーチャー：退避射撃 | 指定敵に12ダメージを与え、その敵から110px退避。通常攻撃より低威力 | 7秒 |
| メイジ：足止め | 指定敵を3秒移動不能にする。既に射程内なら攻撃はできる | 8秒 |
| ヒーラー：障壁 | 指定味方への45ダメージ分を吸収。有効4秒、HP自体は回復しない | 7秒 |
| ヒーラー：緊急回復 | 指定味方を55回復。通常行動が3秒使えなくなる | 8秒 |

割り込み・かばう・ステルス・障壁は通常の行動待ち時間中にも使える「即時対応技」です。
各技の再使用待ちは独立しており、連打はできません。
敵の攻撃には巨兵1.2秒、小型兵0.7秒の予備動作を設け、対象と残り時間を赤い線・文字で表示します。
攻撃間隔は予備動作開始から数えます。挑発は次の狙いを変えますが、既に始まった攻撃をキャンセルしません。

Jevが **技と対象をセットで指定**します。例えば `attack:minion2`、`interrupt:boss`、`retreat:minion4`。
ユーザーが画面で敵を指定する操作はありません。味方対象の技も同様に `cover:healer` のように選びます。
画面の線、予備動作、技のログで狙いを確認できます。

### tactical-v3の動作確認

45秒の実API試験で、割り込み5回、かばう5回、ステルス2回、退避射撃1回、障壁6回、緊急回復1回が発動しました。
足止めも実装・単体テスト済みですが、この試験ではJevは選びませんでした。
終了時はタンク・メイジ・ヒーラーが生存、アーチャーが戦闘不能。巨兵と小型敵1体が残り、時間制限で終了しました。
API76回、平均往復259.2ms。障壁の吸収96.3、かばうによる肩代わり対象ダメージ118.7。
記録: `runs/battle-20260922-140448-210874/`。これは動作確認で、勝率比較ではありません。

| 敵 | 旧HP → 現HP | 旧攻撃力 → 現攻撃力 | 攻撃間隔 |
|---|---:|---:|---:|
| 巨兵1体 | 420 → 1600 | 14 → 64 | 2.4秒 |
| 小型兵4体 | 各55 → 各200 | 5 → 20 | 1.4秒 |

巨兵はヒーラー→メイジ→アーチャー→タンクの順に狙います。
小型兵1・3はメイジ、小型兵2・4はアーチャーを優先し、倒れたら別の後衛へ切り替えます。
挑発の2秒間だけタンクを狙い、その後は後衛狙いに戻ります。
挑発の再使用10秒は通常の技クールダウンとは独立しており、挑発の連打はできません。
敵の強さ・行動・味方の技はJevとbaselineで共通です。

## HARDの比較確認（追加技のない旧hard-v2）

用意した固定ルールのbaseline（弱った敵を攻撃、範囲攻撃、HP割合が低い味方を回復、使えるときに挑発）は、
判断間隔0.25秒・0.5秒・1秒と、応答遅延0秒・0.25秒の4条件で23.4〜34.4秒で敗北しました。
同じ初期配置での決定論的な4条件であり、4回の独立したランダム試験ではありません。
これは既存baselineが勝てないことの確認で、すべてのルールベースAIが勝てないという主張ではありません。
また、Jevが勝てるように敵の数値を切り替える処理はありません。

```powershell
.\.venv\Scripts\python.exe benchmark_battle.py
```

旧比較結果は `runs/battle-hard-baseline.json` に保存しています。
現在このコマンドを実行すると、現在のバージョン名を含む別ファイルに結果を保存します。
旧baselineは攻撃・範囲攻撃・回復・挑発だけのルールでした。
現在のbaselineは `tactical_policy.py` の全スキル対応ルールに更新しています。旧成績とは直接比較できません。

同じHARDルールでJevも1戦確認し、52.8秒で敗北しました（API80回、平均往復234.9ms）。
小型敵4体は倒しましたが、巨兵のHPは1444/1600残りました。
後衛の被ダメージはアーチャー250、メイジ195、ヒーラー160（回復分を含む累計）。
長く生存しただけでは戦術が優れているとはいえず、Jevが勝てる設定であることは確認できていません。
記録: `runs/battle-20260922-135330-706149/`。

Jevが技と対象を選択します。射程までの移動、命中、HP、クールダウン、敵の行動はローカルのゲーム処理です。
同時に行動可能な味方を1回のAPI要求にまとめ、職ごとに独立したchoice質問を送ります。
入力には全員のHP・位置・効果残り時間・現在の対象・各技の再使用待ち・敵の予備動作と対象を含めます。
各質問の選択肢には使用可能な技と対象だけを含めます。
新たな回答が来るまで実行待ちの指示は維持しますが、1指示につき技を使うのは1回です。

通信中も戦闘は30Hzで進みます。要求間隔は最短0.5秒、通信中の要求は1件まで。
1.5秒以上古い回答、一時停止・終了後の回答、倒された対象や使用条件を満たさなくなった技は適用しません。
通常技の待ち時間中でも、使用可能な即時対応技は適用します。
全敵撃破・全味方戦闘不能・90秒経過・API120回で停止し、結果画面を残します。
APIエラーも表示して、認証・課金・制限などのエラーや合計3回のエラーで停止します。

画面はその時点のHPと発動した技を表示します。強化は金色、弱体は紫、防御は水色の輪です。

## 比較・記録

```powershell
# APIを使わない全スキル対応の固定ルール制御（単独実行では通信待ちなし）
.\battle.cmd --controller baseline

# 短いAPI試験と最終画面の保存
.\battle.cmd --headless --duration 20 --max-calls 40 --screenshot runs\battle.png
```

### 同条件での比較

```powershell
.\compare.cmd
# 保存した比較を左右並べて再生（APIなし）
.\compare.cmd --replay latest
```

`compare.cmd` をダブルクリックするとメニューが開きます。「新しい比較を開始（API使用・90秒）」または
「前回の比較を見る（APIなし）」をクリックしてください。起動しただけではAPIを呼びません。
画面下のボタンで再生／一時停止、最初から再生、メニューへ戻る、終了を操作できます。
新しい比較は終了時に結果を保存してメニューへ戻ります。実行中の「終了して保存」は途中経過を保存します。
直接比較を開始する従来の用途には `compare.cmd --live`、画面なしには `--headless` を使えます。

敵・味方の能力、初期配置、利用できる技、観測情報の形式を共通にした2つの戦場を同時進行します。
両者が同じフレームで観測し、Jevの応答が届いたフレームに両者の指示を適用します。
固定ルールAIも全スキルから技と対象を選び、ゲーム内部の未来情報にはアクセスしません。
判断間隔は最短0.5秒、観測から1.5秒以上経過した回答は両者とも破棄します。
Jev側に判断が不要な場合は直前の共通待ち時間（初回は250ms）を再利用します。
共通の90秒上限で比較し、通常モードの120回API上限による途中終了はありません。
APIエラーによる打ち切りは不完全な比較として記録します。

`runs/comparison-日時/` に条件・ソースの写しとハッシュ・両者の観測と判断・リプレイ・サマリー・最終画像を保存します。
再生画面は左Jev、右固定ルールAI。Spaceで停止／再開、Rで最初から、Escで終了します。
この比較は通信待ち時間を揃えた判断方針の比較です。固定ルールAI本来の処理速度を比較するものではありません。
単一配置・1戦の結果だけでモデルの一般的な優劣や勝率は判断できません。

2026-09-22の90秒比較は双方4人生存、小型4体撃破、時間切れでした。
ボス残HPはJev 778、全スキル対応ルールAI 454（初期1600）。敵への総ダメージは1622対1946です。
共通の平均応答待ちは257ms、API137回、エラー0。記録は `runs/comparison-20260922-142510/`。
今回の1戦ではルールAIの方が多く削りました。Jevはステルス9回・防御18回、ルールAIは2回・3回でした。
開始前に停止してリプレイを開くには `compare.cmd --replay latest --paused` を使います。

API呼び出しには利用量が発生します。停止時に通信中だった1件は完了する場合があります。
既存の暗号化済みキーを利用し、チャット・ログ・コマンド引数には出しません。
終了してウィンドウを閉じると `runs/battle-日時/` に判断ログと結果を保存します。
サマリーには技ごとの使用回数、残りHP、応答時間、API使用量を含めます。

今回の戦闘画面はこのPoC用の小さな自作ゲームです。描画はpygame-ceを使用しています。
運転実験と同じ `.venv` で動き、追加パッケージは不要です。

## 初回の実測（強化前の旧ルール）

2026-09-22の1戦では25.9秒で全敵を撃破、味方4人全員が生存しました。
API45回、平均往復232.9ms。挑発5回、単体攻撃26回、範囲攻撃9回、防御1回、回復8回、弱体2回。
味方への強化も選択可能ですが、この戦闘では選ばれませんでした。
入力73,487トークン、出力4,051トークン。
結果ログは `runs/battle-20260922-134627-554793/` に保存しています。
単一の配置で1回試した結果であり、ゲームバランスや戦術の優秀さを評価したものではありません。
